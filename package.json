{
  "dependencies": {
    "@tailwindcss/cli": "^4.0.8",
    "tailwindcss": "^4.0.8"
  },
  "scripts": {
    "compile-css": "npx @tailwindcss/cli -i ./css/input.css -o ./css/style.css --watch",
    "update-repo": "git add . && git commit -m 'update project' && git push origin main"
  }
}
